---
title: "Authentication"
description: "Learn how to authenticate your API requests using API keys"
---

## **API Authentication**

Every API request requires authentication using an API key in the HTTP headers.

## Getting Your API Key

1. Log in to your [TwitterApiIO Dashboard](https://dashboard.twitterapi.io)
2. Find your API key displayed on the dashboard homepage

---

## Using Your API Key

All API requests must include the `x-api-key` header for authentication.
**Header format:**

```bash
x-api-key: YOUR_API_KEY
```
---

**Example requests:**

### cURL

```bash
curl --location 'https://api.twitterapi.io/twitter/user/followings?userName=KaitoEasyAPI' \
--header 'x-api-key: my_test_xxxxx'
```

### Python

```python
import requests

url = 'https://api.twitterapi.io/twitter/user/followings?userName=KaitoEasyAPI'
headers = {'x-api-key': 'my_test_xxxxx'}
response = requests.get(url, headers=headers)
print(response.json())
```

### JavaScript

```javascript
const options = {
  method: 'GET',
  headers: {
    'x-api-key': 'my_test_xxxxx'
  }
};

fetch('https://api.twitterapi.io/twitter/user/followings?userName=KaitoEasyAPI', options)
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error('Error:', error));
```

### Java

```java
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;

OkHttpClient client = new OkHttpClient();

Request request = new Request.Builder()
    .url("https://api.twitterapi.io/twitter/user/followings?userName=KaitoEasyAPI")
    .addHeader("x-api-key", "my_test_xxxxx")
    .build();

try (Response response = client.newCall(request).execute()) {
    System.out.println(response.body().string());
} catch (Exception e) {
    e.printStackTrace();
}
```